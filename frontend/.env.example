# Copy to .env for local overrides. Defaults in src/lib/api/client.ts assume
# UC2's dev port; UC3's backend listens on 8002 (docs/plan.md §7).
VITE_API_BASE_URL=http://localhost:8002/api/v1
VITE_API_KEY=changeme-dev-key
